---
name: grill-with-docs
description: Use before an ambiguous feature, product idea, or architecture/design change in a repository. Interview the user through grilling and domain-modeling, record resolved terms or justified decisions, and stop implementation until shared understanding is reached.
---

This skill is the default repository entrypoint for unsettled feature and design work. It is model-invoked
when its trigger applies and remains available for direct user invocation.

Call the Skill tool twice, for "grilling" and "domain-modeling".
