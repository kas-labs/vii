# CLI detection consumer fixture

This fixture is copied into a clean temporary consumer by package validation. It imports the packed
`@vii-labs/cli-core` artifact and detects the consumer project without executing configuration code or
mutating files.
