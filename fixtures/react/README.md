# Packed React fixture

This clean-consumer fixture exercises the experimental React adapter through a server-rendered
component. The package-validation script copies the source into a temporary consumer, installs the
packed Core and React adapter artifacts, compiles it, and verifies the rendered snapshot.
