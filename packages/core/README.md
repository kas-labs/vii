# @vii/core

This is the first buildable Vii package created by the Phase 0 repository bootstrap.

The package intentionally exposes no runtime API yet. State, Query, UI, adapters, and CLI work
belong to later implementation tasks. The package exists to establish ESM exports, declarations,
package contents, and packed-consumer validation.
